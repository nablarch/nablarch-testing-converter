# task-26 Completion Check

本ファイルは **#26 の最終状態**を記録する。到達までの経過は残さない。

対象は**4 辺の担当クラスの行・分岐カバレッジ計測と、未到達分岐の列挙・分類**。
成果物は `coverage/coverage-report.md`（新規）・`coverage/issues.md` の §7（追記）・
`coverage/inventory.md`（他ファイルの行番号を落とす修正）。

**本ファイルの Self-check 欄・Evidence 欄は、1 巡目（QA ／ Craft ／ Verification）の指摘 F-1〜F-30、
2 巡目の指摘 G-1〜G-20、3 巡目の指摘 A-1〜A-4 ／ B-1〜B-10 ／ C-1〜C-4 ／ D-1〜D-12 への
対応を反映したあとの最終状態である**（対応の一覧は末尾の 3 つの表）。

> **下の「記述規約の自己点検」表と「反映後に流し直した導出コマンドと出力」ブロックは
> 2 巡目（G-1〜G-20）反映時点の記録であり、動かしていない。3 巡目で動いた値の現在値は
> 末尾の「3 巡目レビュー指摘（A-1〜D-12）の対応」節が正である。**

---

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `.rn/ntf-test-data-converter/coverage/coverage-report.md` に対象6区分すべての行・分岐カバレッジ数値が記録されている | OK | 同ファイル §1 に 22 行の表があり、6 区分（① `XlsFormatReader`＋匿名 ／ ② `YamlFormatReader`＋匿名 ／ ③ `XlsFormatWriter` ／ ④ `YamlFormatWriter` ／ ⑤ `TestCoreReaderAdapter` と入れ子 3 クラス ／ ⑥ 中間モデル 12 クラス。`FileDataBlock.FileType` を含む JaCoCo の CLASS 12 件で、導出は `awk -F, 'NR>1 && $2 ~ /converter\.model$/ { print $3 }' target/site/jacoco/jacoco.csv | wc -l` → **12**）を網羅する。区分別合計表も併記（合計 `line 943/959` `branch 493/527`）。§0 に計測時点の HEAD SHA `da6642578c366b73b3a001980142d9f741f82f7e`・実行した 2 つのコマンド・`Tests run: 595, Failures: 0, Errors: 0, Skipped: 2` の実測行・**`Skipped: 2` の内訳**（`@Ignore` 2 件。YML-14 ／ XLS-40。ともに `YamlFormatReaderInvalidInputTest`。**2 件とも辺②のテストクラスであり、あるべき姿を書いたまま無効化されている担保の穴**として §6-6 に開示）を記載。数値の導出 `awk` コマンドを §1 に併記し、そのまま実行して一致することを確認した。**本書の全数値は 1 回の実行が作った `jacoco.csv` ／ `jacoco.xml`（同一実行のペア）だけから導いており**、突き合わせ手段として `md5sum target/site/jacoco/jacoco.csv` → `d28e374e9027ade63d7919f7a7b5826e` を併記した（「2 回実行して一致した」という検証不能な主張は取り下げた）。**100% の区分（辺②・中間モデル）には「100% は担保を意味しない」注記を置き、`inventory.md` §2.1-2 の末尾の #26 宛の指示（カバレッジ数値だけを担保の根拠にしない）に応じた** | OK | 3 巡目（`3b33d5a`）で QA が §1 の 2 本の awk を独立実行し、22 クラス表・区分別合計（`合計 classes 22 line 943/959 branch 493/527 missed line 16 branch 34`）が 1 文字違わず再現。`md5sum` も `d28e374e…` と一致。さらに `target/classes` の 61 クラスと csv の 51 行の差 10 を interface 4 件・`$SwitchMap` 合成 6 件と `javap` で同定し、**JaCoCo が落とした実クラスは無い**ことを確認 |
| 未到達分岐がクラス・メソッド・行番号つきで漏れなく列挙されている | OK | 同 §3 に **34 件**を 1 件ずつ、クラス・メソッド・行番号・**どちらの側が未到達か**まで書いた（3.1 辺① 25 件／3.2 辺③ 3 件／3.3 辺④ 4 件／3.4 `BodyLineCollector` 2 件。辺②・中間モデルは 0 件）。§4 に未到達行 **16 件**も列挙し、**うち 8 行が §3 の分岐に紐づき、残る 8 行（`TestCoreReaderAdapter`）は分岐を持たない未到達行でどの分岐にも紐づかない**ことを明記した。総数の導出は §2 の Python コマンドで、末尾に `{'XlsFormatReader.java': (25, 5), 'XlsFormatWriter.java': (3, 1), 'YamlFormatWriter.java': (4, 2), 'YamlFormatReader.java': (0, 0), 'TestCoreReaderAdapter.java': (2, 8)}` を出す（`25+3+4+0+2 = 34`／`5+1+2+0+8 = 16`）。§1 の区分別合計（分母 527 − 到達 493 = 34、959 − 943 = 16）とも一致する。**§2 のコマンドが出すのは §4 の 16 行の上位集合であり、§3 の「側」「分類」「根拠」はこのコマンドの出力ではない**ことを同節に明記した。「どちら側か」は `mi`／`ci`、消去法、実在テストの 3 つだけを根拠にした。**再現できない使い捨てテストの実験は根拠から外し、実在テストの陽性証人に置き換えた**——`XlsFormatReader.java:585`（#22）は `XlsFormatReaderTest#readTableNormalizesExcelQuotationNotation`（入力 `XlsFormatReaderTest.java:171`・表明 `:179`・呼び出し元 `XlsFormatReader.java:162`）と `#readListMapNormalizesExcelQuotationNotation`（入力 `:196`・表明 `:204`・呼び出し元 `:193`）が `!value.isEmpty()` の両側を通すため、残る `value != null` の false 側が未到達と確定する（`stripQuotes` が `""` を `dropEmptyEntries` の前に空文字へ畳むことを表明 `is("")` が固定している）。`XlsFormatReader.java:510`（#16）も同じく `#readMapsExpectedRequestHeaderMessageBlock`（入力 `:567` `lines.add(row("text-encoding", "ms932"));`・表明 `:584`）で `startsWith("\"")` の false 側の到達を示し、`endsWith("\"")` の false 側が未到達であることを導いた | OK | QA が `jacoco.xml` を自前走査して `mb>0` の行と `mi>0 ci=0` の行を全数抽出し、未到達分岐 34・未到達行 16 を独立に導出。行単位の多重度（L361×2＝#9/#10、L507×2＝#14/#15、L511×3＝#17〜#19）まで §3 と完全一致。`#` は 1〜34 が各 1 回で欠番・重複なし。Verification も同じ抽出を別実装で行い一致。**引用行番号は両者が実物で照合済み** |
| 各未到達分岐が「テストを足すべき」「テスト不要」に分類され、後者には根拠が書かれている | OK | 34 件すべてに分類列と根拠列がある。**テストを足すべき 19 件・テスト不要 15 件**（`19 + 15 = 34`）。導出——`grep -cE '^\| [0-9]+ \|.*\*\*テストを足すべき\*\*'` → **19**、`grep -cE '^\| [0-9]+ \|.*テスト不要（'` → **15**（実行して一致を確認。素朴な `grep -c 'テスト不要（'` は 22 を返すため、行頭を `| 数字 |` に錨で限っている旨を §3 に明記）。**「テスト不要」の理由は §3.0 の凡例で 5 値に閉じ、各行はこの語だけを使う**——型の全数分岐 **3** ／ 中間モデルの不変条件 **2** ／ 内部整合性ガード **4** ／ 実 `.xlsx` では生じない／Fake から到達可 **4** ／ 本体パーサが先に弾く **2**（合計 15。5 本の `grep -cE` を併記し実行して一致）。**3 巡目レビューで「NTF 仕様外」を落とした**——記法が「そう書いてはいけない」と言うことは「書いても分岐に届かない」を示さず、到達可能性の議論になっていなかったため。#12・#13 の到達不能の実体は本体パーサの器の組み立て失敗であり、証人テスト（`XlsFormatReaderInvalidInputTest#failsWhenTypeRowIsShorterThanNameRowInRealBook` ／ `#failsWhenTypeCellIsBlankInMiddleOfTypeRowInRealBook` ／ `#failsWhenLengthRowIsShorterThanNameRowInRealBook`）と `issues.md` C-20 の機構記述・`FieldDef.java:83-84` の生成時拒否へ根拠を張り替えたうえで、`notation:883` ／ `notation:889` は補強材料へ降格した。**「実 `.xlsx` 経路では生じない」5 件のうち #22 は「テストを足すべき」へ移した**——`notation:1535`「全要素が null または空文字のエントリは読み飛ばされる。」が挙動を定めており、かつ `XlsFormatReaderTest.java:141` が既に `null` セルを注入していて `XlsFormatReader.java:157` が `mb=0 cb=2` で両側到達済みだからである（`issues.md` COV-14）。**「到達不能・型／契約に遡る」というラベルは取り下げたまま**——`PoiXlsReader.java:123` は別リポジトリの実装であって converter の型でも Javadoc 契約でもなく、converter の Fake リーダ（`XlsFormatReader.java:89` のパッケージプライベート ctor ＋ `XlsFormatReaderTest.java:54`）経由では到達するため、§6-1 に開示した | OK | 19+15=34、両方に該当する行 0 件。5 値の内訳 3+2+4+4+2=15 も再現。QA は「記法の禁止を到達不能の根拠にすり替える型」を探したが**見つからず**、#12 が自ら「補強材料であって到達不能の根拠ではない」と明記していることを確認。外部根拠（`DataType` 定数 14 個 ／ sealed 宣言 ／ `ModelPreconditions` ／ notation 13 行の逐語）を一次情報で裏取り。**ただし未解決 2 件**——#6 と #22 の分類が同じ論法で割れている（QA-1）、#8〜#11 の行と凡例から §6-5 への参照が 0 件（QA-2） |
| 「テストを足すべき」に分類されたものは、追加されたか `issues.md` へ残課題として記録されたかのいずれかになっている | OK | **19 件すべてを `issues.md` §7 へ `COV-01`〜`COV-14` として記録した**（1 課題が複数分岐にまたがるため件数が異なる）。導出は `grep -c '^### COV-' .rn/ntf-test-data-converter/coverage/issues.md` → **14**、`grep -c '^- 未到達箇所' …` → **14**、分岐数の合計 `awk -F'\|' '/^\| COV-[0-9]+ \|/ { s += $4 } END { print s }' …` → **19**（3 つとも実行して一致）。**`COV-nn` ↔ `coverage-report.md` §3 の `#` の対応表は `issues.md` §7 の 1 か所だけに置き**、`coverage-report.md` §5 はそこを指すポインタにした（`steering.md` Rules「同じ関係を 2 方向に手書きしない」）。テストは足していない（理由は `coverage-report.md` §5 と `issues.md` §7 に記載）。既存の件数コマンド（`grep -c '^### \(XLS\|YML\)-'` → **55**、`grep -c '^- NTF 仕様としての判定'` → **55**、`grep -c '^### XLS-'` → **41**、`grep -c '^### YML-'` → **14**）は `COV-` 前置を数えないため値が変わらないことを実行して確認した | OK | QA が §3 の「テストを足すべき」19 件の集合と `issues.md` §7 の対応表の和集合を `diff` → **IDENTICAL**。`grep -c '^### COV-'`=14、`grep -c '^- 未到達箇所'`=14、分岐数合計=19 も再現。取りこぼしゼロ。Verification も同じ 3 本を独立実行して一致 |
| src/main への変更がゼロ | OK | `git diff HEAD -- src/main pom.xml \| wc -l` → **0**、`git diff HEAD -- src/test \| wc -l` → **0**（使い捨てテストの実験は §2 の記述ごと削除したため、痕跡も残っていない）。`git status --porcelain` は `coverage/coverage-report.md`（M）・`coverage/inventory.md`（M）・`coverage/issues.md`（M）・`checks/task-26.md`（??）の 4 行のみで、`src/`・`steering.md`・他リポジトリはいずれも現れない | OK | QA・Verification とも `git diff --stat da66425 3b33d5a -- src/main` → 0 行、`git status --short` → 0 行を確認。surefire も実測 `run=595 fail=0 err=0 skip=2`、`@Ignore` は `YamlFormatReaderInvalidInputTest.java:740`（YML-14）・`:1280`（XLS-40）の 2 件のみで §0 と一致 |

### 記述規約の自己点検

| 点検項目 | 結果 |
|---|---|
| 併記したコマンドは、そのまま実行して同じ結果が出る | OK。`coverage-report.md` の `awk`（§1 の 2 本・§6-2・§7）・Python（§2）・分類件数の `grep -cE`（§3 の 2 本・§3.0 の 5 本）・`git diff`／`grep -n '^jacoco\.exec$' .gitignore`（§0）・`grep -l 'Skipped: [1-9]'` ／ `grep -rn '@Ignore'`（§0）と、`issues.md` §7 の 3 本（`grep -c '^### COV-'` → 13、`grep -c '^- 未到達箇所'` → 13、分岐数の `awk` → 18）・`grep -nE '^#{2,4} §[0-9]'` を**全件そのまま実行して一致を確認した**。§3 の 2 本は行頭を `\| 数字 \|` に限っている——素朴な `grep -c 'テスト不要（'` は**説明文とコマンド行自身がヒットして 23 を返す**ため（`steering.md` Rules「自分自身がヒットして主張を反証する grep」）。この落とし穴と実測値 23 も同ファイルに明記した（**初版は 20 と誤記していた。文面を直すたびにヒット数が動くため、最後にもう一度数えて 23 を確定させた**）。**G-1〜G-20 の反映後にも全コマンドを流し直し、宣言値と一致することを確かめた**——追加分は `md5sum target/site/jacoco/jacoco.csv` → `d28e374e9027ade63d7919f7a7b5826e`、中間モデルのクラス数 `awk … /converter\.model$/ … | wc -l` → **12**、§6-2 の 14 クラス内訳 `awk`（`n++` でクラス数を導く形に直した）→ `計 14 クラス` ／ 未到達行 **63** ／ 未到達分岐 **16**、§6-3 の 2 本の `grep`（`grep -cE '\.java:[0-9]+' inventory.md` → **8** ／ そのうち `grep -rn` 出力ブロック内のものが **8**）、`inventory.md` の `DirectiveUtil` の `awk` → `line 20/20 branch 17/18`、辺③④の `awk` → `XlsFormatWriter line 157/158 branch 101/104` ／ `YamlFormatWriter line 157/159 branch 86/90`、`git -C ~/work/nablarch/nablarch-testing grep -n 'directives\.put\|directives\.remove' c5f3340 -- 'src/main/java'` → 1 行、`grep -c '〃' coverage-report.md` → **0** |
| 数値・行番号・ファイル名は自分で取得し自分で実行して確かめたものだけを書いた | OK。`jacoco.csv`／`jacoco.xml`／`git show`／各ソースの `sed -n` で全件確認。`notation:nnn` は `git show 30a8271:…/testdata_notation.rst` を取り出して本文を照合した（`:250`・`:252`・`:254`・`:266-267`・`:278`・`:306`・`:622`・`:628`・`:883`・`:889`・`:1325`・`:1397`）。YAML スキーマの `group_id` 4 箇所は `nablarch-testing-yaml` `0197071` の `ntf-testdata-yaml-schema.json` を直接開いて `{"type": "string", "minLength": 1}` だけであることを確かめた。`inventory.md` の行番号を落とす修正では、`0a14655`（追補その 5）・`1b82188`（追補その 6）時点の `XlsFormatWriter.java`／`YamlFormatWriter.java` を `git show` で開き、**その行番号が指していたメソッドを 1 件ずつ同定してからメソッド名に置き換えた** |
| 確かめていないことは「未確認」と明示した | OK。2 件を明示——(1) `-o` を外した場合の再現（§0 の注記・§6-4）、(2) 「内部整合性ガード」に到達する canned 入力（器側と生行側を食い違わせるもの）を実際に組めるかどうか（§6-5）。**初版で「未確認」としていた「`!value.isEmpty()` の false 側を到達させている既存テストの特定」は、実在テスト 2 件を特定できたため取り消した**（§2 の表）。**初版の「MESSAGE の重複識別子の明文は見つけられなかった（未確認）」は取り消した**——`notation:262-270` の収集方式テーブルが「単一」方式（ID が完全一致する最初の 1 件を取得）の該当データタイプに ``LIST_MAP``・``MESSAGE`` を明示しており（`notation:266-267`）、明文は存在する |
| 担保の穴は、テストを足さない場合でも開示した | OK。`coverage-report.md` §6 に **6 項目**を開示し、**性質を取り違えないよう冒頭に性質欄つきの一覧表を置いた**（担保の穴 **4 件**＝§6-1・§6-2・§6-5・§6-6、処置済みの台帳訂正 1 件＝§6-3、確かめきれなかったこと 1 件＝§6-4）——(1) §6-1 「実 `.xlsx` 経路では生じない」5 件が converter の Fake リーダ経由では**到達可能**であること（`issues.md` XLS-09 と同じ内容。`XlsFormatReaderTest.java:141` により `XlsFormatReader.java:157` は `mb=0 cb=2` で両側到達済み）、(2) §6-2 対象 6 区分の**外**に分類していない未到達が分岐 16 件・行 63 件残っており #27 が拾う保証が無いこと（**初版は 5 クラスを名指しするだけで 16 分岐のうち 5 分岐しか説明していなかったため、14 クラスの内訳を出す `awk` とその出力を貼り、`XlsOutputConfig` だけで 8/16 分岐・25/63 行を占めること、その裏返しとして `ExcelFormatConfig` の未到達行 5 件（いずれも `mi=25 ci=0`）が `withXxx` の未呼び出しであることまで書いた**）、(3) §6-3 `inventory.md` から他ファイルの行番号を落としたこと、(4) §6-4 `-o` なしの未確認、(5) §6-5 「内部整合性ガード」4 件の根拠が `src/main` 自身のコメントだけで外部の契約が無いこと（Fake リーダのシームは `XlsFormatReaderTest.java:121-124` に実在し、ガードは `XlsFormatReader#readFileBlocks`／`#readMessageBlock`／`#readSendSyncBlocks` の 3 呼び出し箇所にかかる。**ただし食い違う canned 入力を実際に組めるかは未確認**）、(6) §6-6 `@Ignore` 2 件（YML-14・XLS-40）があるべき姿を書いたまま無効化されていること |
| 触ってよいファイル以外を変えていない | OK。`git status --porcelain` は `coverage/coverage-report.md`・`coverage/inventory.md`・`coverage/issues.md`・`checks/task-26.md` の 4 件のみ。`steering.md`・`src/`・他リポジトリ（`nablarch-testing`／`nablarch-testing-yaml`／`nablarch-document`。いずれも読むだけ）は無変更 |
| `inventory.md` の記述との突き合わせを行った | OK。§4.1-2 の辺③④ JaCoCo 実測（`XlsFormatWriter` `line 157/158 branch 101/104`、`YamlFormatWriter` `line 157/159 branch 86/90`）は本計測と**一致**する。併記されていた未到達の行番号だけが `54d2057`（Javadoc・コメントのみ）による行ずれで古くなっていたため、**行番号を書き直すのではなく落とし、識別をクラス名・メソッド名に変えて、行番号つきの一覧は `coverage-report.md` §3 を指すポインタにした**（`steering.md` Rules「**行番号とファイル行数は**…移動し」。**「6 箇所」という導出の無い件数は取り下げ、確認コマンドを併記した**——`grep -cE '\.java:[0-9]+' .rn/ntf-test-data-converter/coverage/inventory.md` → **8**、そのうち `grep -rn '^    @Ignore' src/test --include=*.java` の凍結出力ブロック内のものが **8**。すなわち**台帳が自分の言葉で主張する他ファイル行番号は 0 件**である。凍結出力を書き直すと「そのまま実行すれば同じ結果が出る」性質が壊れるため、出力は当日のまま残し、その旨を台帳に注記した）。**`DirectiveUtil.java:45` を「未到達行」と書いていた誤り**（実測は `line 20/20` で未到達行 0 件。当該行は部分実行）**も直した** |
| `issues.md` の書式・語彙をそろえた | OK。タイトルを「現状挙動の課題一覧**と、担保の穴の一覧**」に改め、冒頭に `COV-nn` の位置づけを書き、凡例表に `XLS-nn`／`YML-nn` の行と `COV-nn` の行（影響度欄・NTF 仕様としての判定欄を持たないことを明記）を足した。§7 の各項の箇条書きラベルを **`- 未到達箇所:` ／ `- 到達する入力:` ／ `- 根拠:` ／ `- 備考:`（任意）／ `- 処理:` の固定 5 種**にそろえた（実測 13／13／13／**7**／13。COV-11 に足した Excel 形式の制約は、独自ラベルを作らず `- 備考:` に収めた）。§7 冒頭の「§1〜§6」という存在しない節への参照は「本節より前に並ぶ `XLS-nn` ／ `YML-nn` の各項」へ改め、**裸の `§n` が #25.5 の内部ラベルであること**（`§` 付きの `##` 見出しは `§1-D`・`§1-E`・`§6`・`§7` の 4 本だけ）を注記した。XLS-09 の行番号は実測に合わせて **L540／L541／L542**（`da66425` 時点）へ直し、基準 SHA を明記した |


---

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 検証のやり方が目的に対して意味を持っている（「通った」ではなく正しいものを見ている） | OK | 3 巡目（`3b33d5a`）**PASS**。完了条件 5 項目すべて OK。数値・列挙・分類件数・対応関係のいずれも JaCoCo 生データから独立に導いて一致。100% 表記も §1 の注記と `@Ignore` 2 件の開示により「担保」と読めない作りになっていることを確認。**未解決 3 件**は下の「未解決の指摘」表（QA-1・QA-2・QA-4）。QA-3（`checks/task-26.md` が無い）は **Invalid**——同ファイルは実在するが未追跡のため worktree のレビュアには見えない |

## Expert Reviews

### Craft Expert（writing）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 媒体固有のベストプラクティス（散文の明瞭さ・正確さ） | NG | 3 巡目（`3b33d5a`）**FAIL**。完了条件 5 項目は OK だが、**この文書自身が掲げた基準に反する事実の誤りが 2 件**——`coverage-report.md:487` が台帳に存在しない文字列を鉤括弧で引用している（C3-1）、`:82` の「⑤⑥は 4 辺が共有する土台」が同じ文の括弧書きと `src/main` の実装の双方に否定されている（C3-2）。ほか 8 件は下表 |
| 既存の声・用語との一貫性 | NG | 分岐エッジが「出口エッジ」「飛び先」「後続」の 3 語で呼ばれ、助数詞も「件」と「本」で割れている（C3-5）。「裏面」は冒頭だけの語で §4 は同じ関係を「紐づく」と呼ぶ（C3-6）。§3.4 の見出しだけ区分ラベルが落ちている（C3-10） |

### Verification Expert（fact-check）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 成果物が実際に確かめられている（主張が出典と突き合わせて検証されている） | OK | 3 巡目（`3b33d5a`）**PASS**。**文書に併記された導出コマンド全 15 本が宣言どおりの出力を返した。JaCoCo 由来の数値・件数・行番号は 1 件の誤りもゼロ。** notation の引用は `30a8271` から取り出して照合済み |
| 網羅性（主張の取りこぼしが無い） | NG | 引用の逐語性・メソッド行範囲・裏づけの所在に 7 件（下表 V3-1〜V3-7）。うち V3-2 は Craft C3-1 と同一。**未確認として残るもの**——`Tests run: 595 … Skipped: 2` の集計行は当時のビルドログが無く再計測禁止のため直接は確かめられない（surefire レポート側からの傍証は取得済み） |

## 未解決の指摘（修正イテレーション上限 3 回を使い切ったため未処置）

コーディネータが全件を実物で確認し、**Valid**と判定したもの。処置の可否はユーザー判断。

| ID | 出所 | 指摘 | コーディネータの確認 |
|---|---|---|---|
| C3-1 / V3-2 | Craft・Verification | `coverage-report.md:487` が「`DirectiveUtil.java:45` の未到達行」と鉤括弧で引用しているが、この文字列は台帳に存在しない | Valid。`9cc42b2` 時点の `inventory.md:712-713` は「未到達は `DirectiveUtil.java:45` の…**`null` 側 1 分岐**である」で**正しい**。誤っているのは `:721` の「未到達行は L45 のみ」だけ。2 文を継ぎ合わせた合成引用で、台帳の誤りの範囲を実際より広く見せている |
| C3-2 | Craft | `coverage-report.md:82` の「⑤⑥は 4 辺が共有する土台である」が事実に反する | Valid。`src/main` で `TestCoreReaderAdapter` を使うのは `XlsFormatReader`（辺①）だけで、辺②は `YamlTestCoreAdapter` を使う。同じ文の括弧書き「⑤は**辺①が**本体リーダを叩くアダプタ」と矛盾。なお辺②側アダプタの未到達（`line 49/50 branch 18/19`）は §6-2 の 14 クラス内訳に開示済み |
| QA-1 | QA | #6（`readListMapBlock:188`）と #22 の分類が同じ論法で割れている | Valid。双子の `:157` は同一構文（`value == null ? null : stripQuotes(...)`）で `mb=0 cb=2`（Fake テストで両側到達済み）。#6 を「テストを足すべき」へ移さなかった理由が書かれていない |
| QA-2 | QA | #8〜#11（内部整合性ガード）の行から §6-5 への参照が辿れない | Valid（ただし要判断）。`grep -n '§6-5'` は成果物内で 4 行（`:8`・`:300`・`:406`・`:523`）。§3 の表で `§6-5` を持つのは **#5 の 1 行だけ**で、#8〜#11 の 4 行には無い。到達可能性が §6-5 で「未確認」と自認されている 4 件だけが、表からは強い根拠に見える。**逆方向（§6-5 → #5・#8〜#11）の参照は存在する**ため、本件を直すことは案件規約「同じ関係を 2 方向に手書きしない」と衝突する |
| QA-4 | QA | `inventory.md:2923` の「未到達は **3 箇所**」が台帳内で食い違ったまま | Valid。同ファイル `:835`（追補その 5・2026-08-19 実測）は「未到達 行 2・分岐 4 のまま」。B-7 の訂正注記は `issues.md` COV-09 側にだけ付いた |
| C3-3 | Craft | §3 と §7 の「到達する入力」欄が、実行して確かめた事実と同じ文体（断定形）で書かれている | Valid。§5 が「テストは足していない」と述べるとおりコードからの推定であり、その旨の断りが無い |
| C3-4 | Craft | §6 の一覧表の §6-3 行が、体裁の訂正と**事実誤りの訂正**を 1 行にまとめている | Valid。本文（`:487`）は事実誤りの訂正を挙げるが、表の性質欄は「処置済みの台帳訂正」のみ。冒頭 `:9` の「性質が違うものを 1 つの件数にまとめない」に反する |
| C3-5 | Craft | 分岐エッジの呼称が 3 語（出口エッジ／飛び先／後続）に割れ、助数詞も「件」「本」で割れている | Valid（`:162`・`:210`・`:337` ／ `:216`・`:230`・`:311`） |
| C3-6 | Craft | 冒頭 `:4` の「裏面」が未定義で、添えた識別特徴（`throw` ／ `return` 行）が識別になっていない | Valid。紐づかない残り 8 行のうち 4 行（`TestCoreReaderAdapter.java:389`・`394`・`483`・`488`）も `return false;` である |
| C3-7 | Craft | §3.1 #16 が 1 セル 1151 字・太字 8 か所で、同型の証明を §2 では地の文に出している前例と不揃い | Valid。C5（2 巡目）の残り。コーディネータが #31・#34 の 2 件に範囲を切ったため #16 は対象外にした。B-4 の経路訂正で 1064 字 → 1151 字に伸びている |
| C3-8 / V3-7 | Craft・Verification | `:501` の「監査対象に『台帳本文の JaCoCo 数値』を加えた」に検査手段が無い | Valid。`grep -rn '監査'` は成果物ではこの 1 行のみ。§6-3 の確認コマンドは行番号しか見ていない |
| C3-9 | Craft | #21 と #22 を分ける規準が分類欄・凡例から読み取れない | Valid。実際の分け目（記法の明文が挙動を定めているか）は #22 のセル奥と COV-14 の備考にしかない |
| C3-10 | Craft | §3.4 の見出しだけ区分ラベル（区分⑤）が落ちている | Valid。`:289` は「区分⑤（§3.4）」と予告するが `:341` の見出しは素のクラス名 |
| V3-1 | Verification | `:316` の引用「`null` を返すためこのガードは必須」が逐語でない | Valid。実物 `XlsFormatReader.java:540` は「…null を返す**ため、**このガードは必須**。**」。省略記号なしで読点・句点を落としている |
| V3-3 | Verification | `:490` の `steering.md` 引用が括弧書きを省略記号なしで落としている | Valid。実物 `steering.md:56` は「…を書かない**（#23 後の構造見直しで確定・2026-08-13）**。行番号と…」 |
| V3-4 | Verification | `:224` の `stripQuotes` の行範囲が 1 行短い | Valid。宣言 `:539-544` ／ 実物 539〜545。同文書の `dropEmptyEntries`（`:566-574`）・`isEmptyEntry`（`:583-590`）はシグネチャ〜閉じ括弧で書いており不統一 |
| V3-5 | Verification | `:311` の `toStringDirectives` の行範囲が 1 行長い | Valid。宣言 `:467-476` ／ 実物 467〜475（476 は空行） |
| V3-6 | Verification | `notation:nnn` の引用で、RST のインラインエスケープ `\ ` を空白へ均し末尾の「。」を落とす正規化が申告されていない | Valid。`:239` は太字だけを引用者による変形として宣言している |

**Invalid と判定したもの** —— QA-3（`checks/task-26.md` が無い）。同ファイルは実在するが**未追跡**のため、`isolation: worktree` で起動したレビュアの作業ツリーには現れない。1 巡目・2 巡目でも同じ理由で上がっており、判定は 3 巡とも同じ。**本ファイルを追跡下に置いたコミットで原因を解消した**（説明を足すのではなく追跡することで、次巡以降の再燃を止める）。

## 追加 1 巡（ユーザー確定・2026-08-21）の処置

**範囲は A 群 4 件＋B 群 5 件の 9 件に限る。** 上の 18 件の内訳は A 群（事実誤り）4 件・B 群（引用の逐語性）5 件・C 群（用語・体裁）9 件。

| ID | 群 | 処置 | 実物での確認 |
|---|---|---|---|
| C3-1 / V3-2 | A | 合成引用をやめた。誤りの範囲を `9cc42b2` 時点の `inventory.md:721` の括弧書き 1 文「下記 `jacoco.xml` の走査で未到達行は L45 のみ」だけに狭め、その 9 行前の `:712-713` の段落は正しい（分岐について述べている）と明記した | `git show 9cc42b2:.rn/ntf-test-data-converter/coverage/inventory.md \| grep -n '未到達行は L45 のみ'` → `721:`。`:712-713` は逐語で照合済み |
| C3-2 | A | 「⑤⑥は 4 辺が共有する土台である」を「⑤は辺①だけの土台、⑥は 4 辺が共有する土台である」へ直し、辺②が `YamlTestCoreAdapter` を使うこと・同クラスが対象 6 区分の外で §6-2 の 14 クラス内訳に載っていることを併記した | `grep -rln 'TestCoreReaderAdapter' src/main/java/nablarch/test/tool` → `XlsFormatReader.java`・`TestDataBlock.java`（Javadoc の言及）の 2 件 |
| C3-8 / V3-7 | A | 「監査対象に加えた」を実際に行った処置（**計測日を付けた**）の表現へ直し、**台帳本文の JaCoCo 数値を継続的に検査する手段は用意していない（未実施）**と明記した。行番号の側にコマンドがあるのに数値の側に無い理由も書いた | §6 の一覧表 ／ §6-3 の柱書はもとから「計測日を付けた」であり、`:501` だけが実態とずれていた |
| QA-4 | A | 数値を消さず、**それぞれがどの時点の値か**を書き分けた。`inventory.md` §4.1-2 の開示に「ここまでの数値は #25.5 追補（2026-08-18）までの時点」と明記し、行 1・分岐 3 → 行 2・分岐 4 に動いた原因（追補その 5 で `YamlFormatWriter#sectionKey` の `default` が到達不能になった）と現時点の正（§0.1-2 追補その 6）を添えた。§0.1-2 の表側にも逆向きの注記を置いた | 追補その 4 `line 170/171 branch 95/98` → 追補その 5 `line 170/172 branch 98/102`（台帳の「行 1・分岐 1 増えた」の記載）。現時点 `line 157/159 branch 86/90` は `coverage-report.md` §1 の #26 実測と一致 |
| V3-1 | B | `XlsFormatReader.java:540` のコメント引用を逐語（「…`null` を返すため、このガードは必須。」）へ | 実物と逐語照合 |
| V3-3 | B | `steering.md` 引用の落とした括弧書きを省略記号で示した | 実物と逐語照合 |
| V3-4 | B | `stripQuotes` の行範囲 `539-544` → **`539-545`** | 宣言 539・閉じ括弧 545 |
| V3-5 | B | `toStringDirectives` の行範囲 `467-476` → **`467-475`**（476 は空行） | 宣言 467・閉じ括弧 475 |
| V3-6 | B | §3 の出典表記の約束に、`notation` 引用へ掛けている正規化 2 つ（RST のインラインエスケープ `\ ` を半角空白へ均す／文の途中で切る場合は省略記号を置かない）と、入れ子の鉤括弧を『 』へ改めていることを申告した。**あわせて「鉤括弧は逐語」を「出典を添えた鉤括弧は逐語、添えないものは分類ラベル・強調」へ切り分けた** | notation の全引用 12 件を機械照合し、正規化の内容を実測から特定した |

**QA-1 は却下した**（ユーザー判断）。件数は「テストを足すべき 19／テスト不要 15」のまま動かさず、`issues.md` §7 も触っていない。指摘の前提「根拠が書かれておらず」が事実に反し、`coverage-report.md` §3.1 #6 の行には (a)(b) の 2 つの根拠が明記されている。また `:157` は `table.getValue(r, column)`、`:188` は `mapRow.get(column)` で値の出どころが別であり、三項演算子の構文が同じことは `null` の生じ方が同じである根拠にならない。**次巡での再燃を防ぐため、この却下理由を #6 の行に 1 文で残した。**

**C 群 8 件は #27 へ回す**（QA-2・C3-3・C3-4・C3-5・C3-6・C3-7・C3-9・C3-10）。#27 の対応表を書くときにまとめて整える。C 群は当初 9 件だったが QA-1 が却下で閉じたため 8 件。

**引用の機械的な洗い直し（ユーザー指示）。** 処置前の `coverage-report.md` から鉤括弧 81 か所を全数抽出し（`python3` で `「…」` を走査）、**出典（ファイル名・行番号・文書名）を伴う 33 件**を原典と照合した——`notation`（`30a8271`）13 件・`src/main` ／ `src/test` 11 件・`issues.md` 4 件・`inventory.md` 2 件・`steering.md` 3 件。上の B 群 5 件に加え、**新たに 2 件の非逐語引用を見つけて直した** —— (1) `TestCoreReaderAdapter#markerGroupId` の説明「データタイプ名の直後から `=` まで」は原典に無い要約だったので鉤括弧を外し、要約である旨を明記した。(2) 台帳へ残した文言の引用を逐語（「**行としては到達済み**——`mb=1 cb=1`・`ci=9` の部分実行であって未到達行ではない」）へ直した。

**処置後に流し直した導出コマンド**（すべて宣言どおりの出力）:

```
git diff HEAD -- src/main src/test pom.xml | wc -l                                     → 0
grep -cE '^\| [0-9]+ \|.*\*\*テストを足すべき\*\*' coverage-report.md                    → 19
grep -cE '^\| [0-9]+ \|.*テスト不要（' coverage-report.md                                 → 15
grep -cE '^\| [0-9]+ \|.*テスト不要（型の全数分岐）' coverage-report.md                     → 3
grep -cE '^\| [0-9]+ \|.*テスト不要（中間モデルの不変条件）' coverage-report.md             → 2
grep -cE '^\| [0-9]+ \|.*テスト不要（内部整合性ガード）' coverage-report.md                 → 4
grep -cE '^\| [0-9]+ \|.*テスト不要（実 `\.xlsx` では生じない／Fake から到達可）' …          → 4
grep -cE '^\| [0-9]+ \|.*テスト不要（本体パーサが先に弾く）' coverage-report.md              → 2
grep -c '100 %' coverage-report.md                                                     → 0
grep -cE '\.java:[0-9]+|\bL[0-9]{2,4}\b' inventory.md                                  → 8
grep -E  '\.java:[0-9]+|\bL[0-9]{2,4}\b' inventory.md | grep -cE '^(② |   )src/test/java/' → 8
```

## Overall Verdict

- Self-check: OK（レビュー指摘 F-1〜F-30 ／ G-1〜G-20 ／ 3 巡目 A-1〜D-12 ／ 適用漏れ R-1・R-2 ／ 追加 1 巡の A 群 4 件・B 群 5 件 反映後）
- QA: OK（PASS。QA-1 はユーザー判断で却下・却下理由を成果物に記録、QA-3 は無効・原因の未追跡を解消、QA-4 は処置済み。**未解決は QA-2 の 1 件で #27 へ**）
- Design expert: N/A（本タスクは構造・方針を作らない）
- Craft expert: **必須 2 件（C3-1・C3-2）は処置済み。** C3-8 も処置済み。**未解決は C3-3〜C3-7・C3-9・C3-10 の 7 件で、いずれも用語・体裁。#27 へ**
- Verification expert: OK（PASS。V3-1〜V3-7 は全件処置済み）
- Ready to check off: **Yes**（ユーザー確定・2026-08-21。追加 1 巡の範囲を A 群 4 件＋B 群 5 件に限り、C 群 8 件を #27 へ回すことを含めて承認された）

---

## レビュー指摘（F-1〜F-30）の対応

**採否: 対応 30 件 ／ 未対応 0 件。** 分類の変更を伴ったのは F-5・F-6・F-7 の 3 件で、
これにより「テストを足すべき」は 15 → **18**、「テスト不要」は 19 → **16** に動いた
（合計 34 は不変。カバレッジ数値そのものは再計測不要と確認済みで、変えていない）。

| # | 指摘の要点 | 対応 |
|---|---|---|
| F-1 | `grep -c 'テスト不要（'` の期待値 20 が誤り（実測 23）。説明「本コマンド行自身」も単数で誤り | 対応。**23** へ直し、「説明文とコマンド行自身がヒットする」へ改めた。文面を直すとヒット数が動くため、最終文面で数え直して確定 |
| F-2 | 「MESSAGE の重複識別子の明文は見つけられなかった（未確認）」は誤り | 対応。`notation:266-267`（収集方式テーブルの「単一」方式に ``LIST_MAP``・``MESSAGE`` を明示）を根拠に置き換え、**未確認を撤回**。§6 の開示 2 を削除し、`issues.md` COV-01 も同様に直した |
| F-3 | COV-09 の「辺④は `inventory.md` でも開示されていなかった」は誤り | 対応。`inventory.md` §4.1-2 の「開示」が未到達 3 箇所（`write` の `parent` ガードを含む）を既に開示していることを確かめ、**取り消した旨を明記**して備考へ書き直した |
| F-4 | #22 の根拠のうち `:162`（テーブル系）側が成り立たない（`:157` は `mb=0 cb=2` で両側到達済み） | 対応。分類は「テスト不要」のまま、根拠を書き直した——呼び出し元 2 箇所の要素の出どころを正しく書き、**`:157` が Fake リーダにより両側到達済みであること**を明記して §6-1 へ相互参照した（実 `.xlsx` では `null` が生じないという側の判定は変えていない） |
| F-5 | 「到達不能・型／契約に遡る」ラベルが 5 件（#6・#7・#14・#21・#22）で成り立たない | 対応。ラベルを「**実 `.xlsx` 経路では生じない**（`PoiXlsReader.java:123`）。converter の Fake リーダ経由では到達可能」へ改め、**§6-1 として開示**した（分類は「テスト不要」のまま） |
| F-6 | #24・#25 と #26・#29 が同型なのに分類が割れている | 対応。**#24・#25 を「テストを足すべき」へ移し**、`issues.md` §7 に **COV-12** を追加。Javadoc（`XlsFormatReader.java:690`／`:701`）が振る舞いを明記していること、および XLS-21 の【判定の訂正】が「到達経路が無い」を根拠にしないと定めていることを根拠にした |
| F-7 | #31 の根拠が XLS-39 の【判定の訂正】で既に退けられた推論 | 対応。**「テストを足すべき」へ移し**、`issues.md` §7 に **COV-13** を追加。根拠——(a) 辺①から作れる（`SETUP_TABLE[abc=T` → `groupId` = `"[abc"`。XLS-11 に記録済み）、(b) 中間モデルの契約は `null` を禁じるだけ、(c) YAML スキーマの `group_id` は 4 箇所すべて `{"type":"string","minLength":1}` のみ、(d) XLS-39 が保留にしているのは `[a]x[b]`（true 側）であり false 側は往復が一意なので今日固定できる |
| F-8 | 100% を「カバレッジ数値だけを担保の根拠にしない」注記なしで書いている。`Skipped: 2` の内訳も無い | 対応。§1 に注記（辺②は 100% でも変異が 3 件生存した実績・`@Ignore` 2 件が残っていること）を置き、§0 に `Skipped: 2` の内訳表（課題 ID・メソッド・位置・他責先）と導出コマンドを足した |
| F-9 | §5 の柱書「すべて §4 のいずれかの分岐に紐づく」が本文と矛盾 | 対応。「16 行のうち 8 行は §3 の分岐に紐づき、残る 8 行は分岐を持たない未到達行でどの分岐にも紐づかない」へ書き直した |
| F-10 | §3 のコマンド説明が誤り（§5 の上位集合を出し、§4 の側・分類・根拠は出さない） | 対応。「印字される行は §4 の 16 件の**上位集合**」「**§3 の側・分類・根拠はこのコマンドの出力ではない**」を明記し、側の決め方 3 種を別立てにした |
| F-11 | §0 のワーキングツリー欄がコミット後に偽になる | 対応。「**計測時点でクリーン**（`git status --short` が無出力）。`src/` は最後まで無変更」へ改め、検証は `git diff HEAD -- src/main src/test pom.xml` に置き換えた |
| F-12 | `issues.md` §7 の「§1〜§6」は存在しない節への参照 | 対応。「本節より前に並ぶ `XLS-nn` ／ `YML-nn` の各項」へ改め、裸の `§n` が #25.5 の内部ラベルであることを `grep -nE '^#{2,4} §[0-9]'` の実測（4 本）とともに注記した |
| F-13 | `inventory.md` の他ファイル行番号が `54d2057` で陳腐化 | 対応。**書き直さず落とした**。5 箇所（追補その 5 の実測ブロック／辺③の安全網の記述／番人の移設前実測／`sectionKey` の開示／追補その 6 の実測ブロックと直後の散文／§3.1-2 の表）を**クラス名・メソッド名**に置き換え、行番号つきの一覧は `coverage-report.md` §3 を指すポインタにした。置き換えにあたり `0a14655`・`1b82188` 時点のソースを `git show` で開き、旧行番号が指すメソッドを 1 件ずつ同定した |
| F-14 | `issues.md` XLS-09 の L531／L533 が実測と違う | 対応。**L540／L541／L542**（`da66425` 時点）へ直し、訂正した旨と基準 SHA を引用ブロックで記録した |
| F-15 | 対応表が 2 か所に重複 | 対応。本体を `issues.md` §7 に一本化し、`coverage-report.md` §5 はポインタだけにした |
| F-16 | 6 区分外の未到達（分岐 16・行 63）が未開示 | 対応。§6-2 として開示し、導出 `awk` を併記。辺③④の変換ルールを実際に持つ 5 クラスを名指しし、**#27 が拾う保証は無い**と明記した |
| F-17 | 「テスト不要」の理由の凡例が無い | 対応。§3.0 として 5 値の凡例表と件数（3／2／4／5／2＝16）を置き、5 本の `grep -cE` を併記した |
| F-18 | 「NTF 記法外」は本書だけの新造語 | 対応。既存語の「**NTF 仕様外**」へそろえ、その旨を注記した |
| F-19 | §2 の反転 `awk` が書き下されていない | 対応。§6-2 と §7 の両方に、条件を `!( … )` で反転した `awk` をそのまま実行できる形で書いた |
| F-20 | 柱書が結論を述べていない | 対応。冒頭 4 行を結論（34／16、18／16、`COV-01`〜`COV-13`、100% は担保でない、開示 4 件）にした |
| F-21 | 29 クラスの参考表が本論の途中にある | 対応。**末尾（§7）へ移した** |
| F-22 | §1 の合計を手計算で書いている | 対応。区分別合計を出す `awk` を併記し、**その出力そのもの**を貼った |
| F-23 | `issues.md` §7 の「計 15」に導出が無い | 対応。`awk -F'\|' '/^\| COV-[0-9]+ \|/ { s += $4 } END { print s }'` → **18** を併記した |
| F-24 | 実行できないコマンドが 2 つ、`.gitignore` の確認が不正確 | 対応。`\|` のエスケープを除き、`git checkout --` にパスを補い（`git checkout -- src/test`）、`.gitignore` の確認を `grep -n '^jacoco\.exec$' .gitignore` → `3:jacoco.exec` にした |
| F-25 | COV-09 に「到達する入力」が無い。§7 のラベル語彙が 5 通りに割れている | 対応。COV-09 に到達する入力を書き、**全 13 項のラベルを固定 5 種**にそろえた（`grep -c '^- 未到達箇所'` → 13） |
| F-26 | `issues.md` のタイトル・柱書・凡例が `COV-nn` を含んでいない | 対応。タイトルを「現状挙動の課題一覧と、担保の穴の一覧」に改め、柱書と凡例表に `COV-nn` の行を足した |
| F-27 | 表の列見出しの粒度 | 対応。`coverage-report.md` の各表の見出しを「行（到達/全体）」「分岐（到達/全体）」「未到達の側」など、単位が読み取れる語にそろえた |
| F-28 | `100 %` の表記ゆれ | 対応。`100%` に統一（`grep -n '100 %'` → 0 件） |
| F-29 | 細かな事実の誤り（`:490` の開始位置、sealed 階層の説明不足、具象サブクラスは 4 つ、`read` の宣言行、「2 回」計測と `Tests run: 596` 実験の書き分け） | 対応。`XlsFormatReader.java:490-492`、sealed の `permits` を 2 段とも引用して**具象は 4 クラス**と明記、`read` の宣言位置、§0 の「2 回実行して一致」と §2 の使い捨てテスト実験（`Tests run: 596`）を別の記述として分けた |
| F-30 | `isEmptyEntry` の実験の扱い | 対応。実験を主たる根拠として残したうえで、QA の消去法（既存テストのどれかが `!value.isEmpty()` の false 側を通しているはず）を裏づける既存テストを**特定できなかった**ため、その旨を §2 に「未特定である」と明記した |

**コミット。** `03e74be`（F-1〜F-30 の反映。`coverage-report.md`・`issues.md`・`inventory.md`）と
`c8ead78`（課題 ID の前置による内訳 XLS 41 ／ YML 14 を導出コマンド付きで台帳へ記録）。
いずれも `git push` 済み（force なし）。**`6a1924f` のメッセージの誤り（「XLS/YML の件数（各 55）」。
55 は合計であって各前置の件数ではない）は履歴を書き換えず、`c8ead78` で台帳側に正しい数値を残した。**
本ファイルはステージもコミットもしていない（`git status --porcelain` に `??` として残る）。

---

## レビュー指摘（G-1〜G-20）の対応

**採否: 対応 20 件 ／ 不採用 0 件。** 2 巡目の判定は QA＝合格（条件付き）・Craft＝不合格・Verification＝不合格で、
**3 者とも「数値の芯に欠陥は無い」と確認している**。よって**再計測は行わず、数値・件数・分類・「どちら側が未到達か」の
判定は 1 つも動かしていない**（未到達分岐 34 ／ 未到達行 16 ／ 分類 18・16 ／ 区分別合計 `line 943/959`
`branch 493/527` はいずれも据え置き。反映後に併記コマンドを全件流し直して一致を確認した）。
落ちていたのは**出典の精度**と**開示の網羅性**である。

**指示側の誤りも 1 件あった。** 前巡の指示は「`issues.md` の XLS-21 の【判定の訂正】」と書いていたが、
実物の【判定の訂正】は **XLS-20** にある。`coverage-report.md` §3.1 #24 の根拠欄と `issues.md` COV-12 の
備考欄の 2 箇所を XLS-20 へ直した。**それ以外の XLS-21 参照（カラム名 0 件の件）は正当なので残した。**

| # | 指摘の要点 | 対応 |
|---|---|---|
| G-1 | `inventory.md` が `DirectiveUtil` の未到達を「`DirectiveUtil.java:45` の未到達行」と書いているが事実誤り。実測は `line 20/20` で未到達行 0 件、当該行は部分実行 | 対応。行番号を落として `DirectiveUtil#toStringDirectives` の三項演算子と書き、**行は 20/20・未到達行 0 件・これは部分実行であって未到達行ではない**ことを明記した。同節の「未到達行は L45 のみ」も「`mb > 0` の行は三項演算子の 1 行だけ」へ直し、`coverage-report.md` §6-3 にも事実誤りとして開示した |
| G-2 | `inventory.md` §3.1-2 の表が `layoutColumnRow` のコメントを「カラム名が `null` の場合は…非マーカーとして扱う」と引用しているが、実物にその文は無い | 対応。実物（`XlsFormatWriter#layoutColumnRow`）のコメントをそのまま引用に置き換え、**不到達の理由が中間モデルの不変条件（`ModelPreconditions#requireNoNulls`）であること**まで書いた。`（1 / 6）` などの数値は不変 |
| G-3 | `coverage-report.md` が指す `inventory.md` §2.2 は存在しない | 対応。実在する `inventory.md` §2.1-2 の末尾へ参照を付け替えた |
| G-4 | §3.1 #16 の「消去法」が成り立たない（引ける前提が揃っていない） | 対応。**消去法をやめ、陽性証人で導いた**。`XlsFormatReaderTest#readMapsExpectedRequestHeaderMessageBlock`（入力 `XlsFormatReaderTest.java:567` `lines.add(row("text-encoding", "ms932"));`／表明 同 `:584`）が `XlsFormatReader.java:246 → :467-476 → :472 → :482 → :493` の経路で `startsWith("\"")` の false 側を通す。`L494` が `mi=0 ci=3` で到達済み、`L511`（全角側）は `mb=3 cb=1` で true 側が一度も立たないため `L494` へは `L510` 経由でしか入れない。よって `startsWith` の両側と `endsWith` の true 側が到達済みで、残る `endsWith("\"")` の false 側が未到達と確定する |
| G-5 | 逐語引用が実物と食い違う 6 件 | 対応。6 件すべて実物を開いて差し替えた——#1・#2 の `processed.add(singleKey(type, header.getIdentifier()))`（囲む `else if` まで併記）、#5 の `DEFAULT` ガード（3 行そのまま）、#27 の `ColumnRowDataBlock.java:67-68`（`extends TestDataBlock permits …` を含む宣言）、#24 の Javadoc（`{@code '/'}` の表記まで一致）、`issues.md` COV-09 で落ちていた「（sealed 階層の安全網。唯一の未到達行はここ）」、`steering.md` の「**行番号とファイル行数は**…移動し」 |
| G-6 | 出典の範囲・抜粋の 3 件（造語「収集方式テーブル」、範囲の取り違え、Excel 限定の制約の欠落） | 対応。`notation:256` の本文＋表（`notation:258-270`）＋「単一」方式の定義（`notation:266`）と該当データタイプ欄（`notation:267`）を分けて示し、**その欄が「など」で閉じており列挙は網羅でないこと**を明記した。#34 は `notation:252`・`notation:254` を挙げ、`notation:276` 節の `.. important::`（`notation:280`）が置く Excel 形式限定の制約（`notation:306`）を逐語で足した。`issues.md` COV-11 にも同じ制約を `- 備考:` として入れた |
| G-7 | 台帳には `.java:NNN` がまだ 13 件あるのに、`coverage-report.md` §6-3 は「6 箇所を落とした」と書いている | 対応。**台帳が自分の言葉で主張していた行番号は全部落とした**（`@Ignore` 2 件の `（L639）`／`（L1002）`、`XlsFormatWriterInvalidOutputTest.java:91`、`DataFile.java:56`、`TestCoreFileAdapter.java:51`、`DataFile.java:305`、`（同 L304）`）。残る 8 件は `grep -rn` の**出力そのもの**（記録した日の実測）であり、書き直すと「そのまま実行すれば同じ結果が出る」という記録の性質が壊れるため凍結し、**その理由を台帳に注記した** |
| G-8 | §6-2 の「対象外 16 分岐」の説明が 5 分岐ぶんしか無い | 対応。未到達を持つクラスを**全件**出す `awk`（クラス数も `n++` で導く）とその出力を貼り、`XlsOutputConfig` 1 クラスで **8/16 分岐・25/63 行**を占めること、未到達分岐 8 件がすべて `toExcelFormatConfig()` の色・幅オプションの true 側（`L134`・`L140`・`L143`・`L146`・`L149`・`L155`・`L158`・`L161`。いずれも `mb=1 cb=1`）であること、その裏返しで `ExcelFormatConfig` の 5 行（`mi=25 ci=0`）が未到達であることを書いた |
| G-9 | 「内部整合性ガード」4 件の根拠が `src/main` 自身のコメントだけである点が未開示 | 対応。**§6-5 として新設**。他の 4 分類が外部（記法・型・別リポジトリの実装）に根を持つのに対し、この 4 件だけは `src/main` のコメントが唯一の根拠であることを書いた。ガードは `XlsFormatReader#readFileBlocks`（`:210`／`:212`）・`#readMessageBlock`（`:236`／`:239-241`）・`#readSendSyncBlocks`（`:272`／`:273-274`）の 3 呼び出し箇所にかかり、Fake リーダのシーム（`XlsFormatReaderTest.java:121-124`）は実在する。**ただし「器側と生行側を食い違わせる canned 入力を実際に組めるか」は #26 では確かめていない**ことを未確認として明示した（分類は「テスト不要」のまま） |
| G-10 | 「同じ条件で 2 回実行し全数値が一致した」は読み手が検証できない | 対応。**主張を取り下げ**、「全数値は上の 1 回の実行が作った `jacoco.csv` ／ `jacoco.xml`（同一実行のペア）だけから導いている」に改め、突き合わせ手段として `md5sum target/site/jacoco/jacoco.csv` → `d28e374e9027ade63d7919f7a7b5826e` を併記した |
| G-11 | §2 の「使い捨てテストを足して再計測した」実験は、テストが残っていないため再現できない | 対応。**実験そのものを削除**し、実在テスト 2 件の陽性証人に置き換えた（`#readTableNormalizesExcelQuotationNotation`：入力 `XlsFormatReaderTest.java:171`／表明 `:179`／呼び出し元 `XlsFormatReader.java:162`、`#readListMapNormalizesExcelQuotationNotation`：入力 `:196`／表明 `:204`／呼び出し元 `:193`）。`stripQuotes`（`XlsFormatReader.java:539-544`）が `""` を `dropEmptyEntries` の前に空文字へ畳むため、1 要素目で `!value.isEmpty()` の false 側、2 要素目で true 側を通る。3 本到達済みなので残る `value != null` の false 側が未到達と確定する |
| G-12 | 「（`inventory.md` の 6 箇所）」に導出が無い | 対応。件数を手で書くのをやめ、**その場で実行できる 2 本の `grep`** に置き換えた——`grep -cE '\.java:[0-9]+' inventory.md` → **8**、うち `grep -rn '^    @Ignore' src/test --include=*.java` の凍結出力ブロック内が **8**。すなわち台帳が主張する他ファイル行番号は **0 件** |
| G-13 | §6 が性質の違うもの（穴・処置済み・未確認）を 1 つの件数にまとめている | 対応。冒頭に**性質欄つきの 6 行の一覧表**を置き、担保の穴 **4 件**（§6-1・§6-2・§6-5・§6-6）／処置済みの台帳訂正 1 件（§6-3）／確かめきれなかったこと 1 件（§6-4）に分けた。既存の相互参照が生きるよう **新設分は §6-5・§6-6 として末尾に足した** |
| G-14 | 柱書の 1 文に「16」が 3 つ入り読み取れない | 対応。文を分け、未到達分岐 34 と分類 18／16、未到達行 16 を別の文にした |
| G-15 | `〃` が姉妹文書（`inventory.md`／`issues.md`）と不統一 | 対応。§1 の表の `〃` 16 箇所をすべて `同上` に置き換えた（`grep -c '〃'` → **0**） |
| G-16 | 中間モデルを「11 クラス」と書いているが JaCoCo の CLASS は 12 件 | 対応。**12 クラス**に直し、`awk -F, 'NR>1 && $2 ~ /converter\.model$/ { print $3 }' target/site/jacoco/jacoco.csv \| wc -l` → **12** を併記した |
| G-17 | §0 の `@Ignore` 表の「位置」欄（`…java:740` ／ `:1280`）は行ずれで腐る | 対応。**「位置」欄を「テストクラス」欄に替え**（`YamlFormatReaderInvalidInputTest` ／ 同上）、導出コマンドはそのまま残した。あわせて**この 2 件が担保の穴である**ことを明記し §6-6 へつないだ |
| G-18 | 「区分①」と「辺①」を二重に書いており、どちらが主かわからない | 対応。§1 の冒頭に「**区分①〜④が 4 辺そのもので、⑤⑥は 4 辺が共有する土台である**。以降この 6 つを『対象 6 区分』と呼び、辺番号は再掲しない」という橋渡しの 1 文を置き、区分欄から辺番号を外した。合計 `awk` のラベルも同様に直し、**流し直して数値が変わらないことを確かめた** |
| G-19 | §2 の Python の出力が 3 行で貼られているが `print(tot)` は 1 行で出す | 対応。「**`print(tot)` は dict を 1 行で出力する。下は読みやすさのために 3 行へ折り返したものであり、折り返し以外は出力そのままである**」と明記した（貼った内容は変えていない） |
| G-20 | §3.1〜§3.4 の「分類」欄から §3.0 の凡例への相互参照が無い（任意） | 対応。§3 の柱書に「『テスト不要』側の 5 値の意味と件数は次の §3.0 の凡例にある。§3.1〜§3.4 の各表の『分類』欄はこの 5 語だけを使う」を足した。**足した文が件数コマンドのヒット数を動かさないことを流し直して確認した**（strict 18／16、素朴 23 のまま） |

### 反映後に流し直した導出コマンドと出力

```
$ git diff HEAD -- src/main src/test pom.xml | wc -l
0
$ md5sum target/site/jacoco/jacoco.csv
d28e374e9027ade63d7919f7a7b5826e  target/site/jacoco/jacoco.csv
$ awk -F, 'NR>1 && $2 ~ /converter\.model$/ { print $3 }' target/site/jacoco/jacoco.csv | wc -l
12
$ awk -F, '…6 区分の外で未到達を持つクラスを全件…' target/site/jacoco/jacoco.csv | tail -1
計 14 クラス               missed_line=63  missed_branch=16
$ awk -F, 'NR>1 && $3 == "DirectiveUtil" {…}' target/site/jacoco/jacoco.csv
line 20/20 branch 17/18
$ awk -F, 'NR>1 && ($3 == "XlsFormatWriter" || $3 == "YamlFormatWriter") {…}' target/site/jacoco/jacoco.csv
XlsFormatWriter line 157/158 branch 101/104
YamlFormatWriter line 157/159 branch 86/90
$ F=.rn/ntf-test-data-converter/coverage/coverage-report.md
$ grep -cE '^\| [0-9]+ \|.*\*\*テストを足すべき\*\*' $F ; grep -cE '^\| [0-9]+ \|.*テスト不要（' $F
18
16
$ grep -c 'テスト不要（' $F        # 素朴版（説明文とコマンド行自身がヒットする）
23
$ 5 値の内訳（型の全数分岐／中間モデルの不変条件／内部整合性ガード／実 .xlsx 経路では生じない／NTF 仕様外）
3 2 4 5 2
$ grep -c '〃' $F
0
$ I=.rn/ntf-test-data-converter/coverage/inventory.md
$ grep -cE '\.java:[0-9]+' $I
8
$ grep -E '\.java:[0-9]+' $I | grep -cE '^(② |   )src/test/java/'
8
$ git -C ~/work/nablarch/nablarch-testing grep -n 'directives\.put\|directives\.remove' c5f3340 -- 'src/main/java' | wc -l
1
$ J=.rn/ntf-test-data-converter/coverage/issues.md
$ grep -c '^### COV-' $J ; grep -c '^- 未到達箇所' $J ; awk -F'|' '/^\| COV-[0-9]+ \|/ { s += $4 } END { print s }' $J
13
13
18
$ grep -c '^- 備考:' $J
7
$ grep -c '^### \(XLS\|YML\)-' $J
55
```

**コミット。** `a25ca89`（G-1〜G-20 の反映。`coverage-report.md`・`issues.md`・`inventory.md` の 3 件のみを
パス明示でステージ）。`git push` 済み（force なし。`c8ead78..a25ca89`）。
**本ファイルはステージもコミットもしていない**（`git status --porcelain` に `??` として残る）。
`src/main`・`src/test`・`pom.xml` は無変更（`git diff HEAD -- src/main src/test pom.xml | wc -l` → 0）。
`steering.md` と他リポジトリにも書き込んでいない。

---

## 3 巡目レビュー指摘（A-1〜A-4 ／ B-1〜B-10 ／ C-1〜C-4 ／ D-1〜D-12）の対応

**29 件すべてを実物で確かめたうえで反映した。うち 2 件は指摘の一部が実物と食い違っていたため、
指摘の意図を満たしつつ実測に合わせて直した**（A-2／A-4 の「6 値」と B-7 の「追補その 6」。下表の ⚠ 行）。

| # | 指摘の要点 | 対応 | 確認した実物 |
|---|---|---|---|
| A-1 | #12・#13 の「NTF 仕様外」は到達可能性の議論になっていない | 対応。分類は「テスト不要」のまま、根拠を「本体パーサが先に弾く」へ差し替え、証人テスト 3 件と `issues.md` C-20・`FieldDef` の生成時拒否を明記。`notation:883`／`:889` は補強材料へ降格 | `XlsFormatReaderInvalidInputTest.java` の Javadoc（`:944` 前の「そこへ届く前に本体パーサが器の組み立てに失敗する」／`:834` 前の「型行・長さ行の不一致は本体パーサが弾く」）を逐語照合。`FieldDef.java:83-84` に `if (type == null) { throw new IllegalArgumentException(`。`issues.md:646` の C-20 行 |
| A-2 ⚠ | 凡例に「本体パーサが先に弾く」を足し「NTF 仕様外」を落とす（指摘は結果を「6 値」と書いている） | 対応。値を足して落とした。**ただし 5 値 − 1 + 1 = 5 値であり 6 にはならない。**凡例表は 5 行、`grep -cE` も 5 本、合計 15 で整合する。本文の「6 値／6 語」は実測に合わせて **5 値／5 語**とした | 凡例表を数えて 5 行。5 本の `grep -cE` の実行結果 `3 / 2 / 4 / 4 / 2`（合計 15）。`19 + 15 = 34` |
| A-3 | #22 は記法の明文が挙動を定めており、スイートが既に `null` セルを注入している | 対応。#22 を「テストを足すべき」へ移し、`issues.md` §7 に `COV-14` を新設（欄は既存 COV 項と同じ 5 種） | `notation:1535`（`30a8271`）「全要素が\ null\ または空文字のエントリは読み飛ばされる。」。`XlsFormatReaderTest.java:141` `lines.add(row("literal", null));` ／ `:153` コメント ／ `:155` `is(nullValue())`。`jacoco.xml` の `XlsFormatReader.java` `L157` は `mi=0 ci=10 mb=0 cb=2`、`L585` は `mb=1 cb=3` |
| A-4 ⚠ | 件数を全部数え直す | 対応。全件コマンドで導出し直した——足すべき **19** ／ 不要 **15**（型の全数分岐 3・中間モデルの不変条件 2・内部整合性ガード 4・実 `.xlsx` では生じない／Fake から到達可 4・本体パーサが先に弾く 2）／素朴 `grep -c 'テスト不要（'` **22** ／ `COV-` 項 **14** ／ COV の分岐計 **19**。**「5 値の内訳 → 6 値」の部分だけは A-2 のとおり 5 値** | 上記すべてを実行。`XLS-`/`YML-` 側の既存件数（55／55／41／14／25／27／2／1）も流し直して不変を確認 |
| B-1 | `inventory.md` に `L155` 形式の反例が残っている／§6-3 の確認コマンドが拾えない | 対応。`inventory.md` の当該 `。L155` を落とし、確認コマンドを `grep -cE '\.java:[0-9]+\|\bL[0-9]{2,4}\b'` へ広げた。**開示文に `L155` を literal で書くとコマンドが自分自身を拾って 9 を返したため、「`L` に続けて行番号を書く形式」という言い回しに改めた** | 修正後に実行して **8 / 8** で一致。残る 8 件はすべて `grep -rn '^    @Ignore'` の凍結出力ブロック内（`inventory.md:340-341`・`:580-582`・`:802-804`・`:884-886`） |
| B-2 | 台帳の辺② JaCoCo 数値 2 箇所が古く互いに食い違う | 対応。両方を計測日つきの過去形へ改め、現行値 `line 192/192 branch 102/102` を併記。**「未到達 0 件＝100%」という論旨は 3 つとも同じなので変えていない。**§6-3 の監査対象に「台帳本文の JaCoCo 数値」を加えたことを開示した | `jacoco.csv` の `YamlFormatReader` 行から `line 192/192 branch 102/102`。台帳の 200/200・106/106（追補その 2 の比較表と整合）と 201/201・108/108 |
| B-3 | `DirectiveUtil` のポインタが §3 を指すが §3 に載っていない | 対応。ポインタを §6-2・§6-3 へ張り替え、台帳側には「行としては到達済み（`mb=1 cb=1`・`ci=9`）」だけを残した | `sed -n '232,345p' coverage-report.md \| grep -c DirectiveUtil` → 0（§3 は `:232` の見出しから `:345` まで）。`jacoco.xml` の `DirectiveUtil.java` は `line 20/20` |
| B-4 | #16 の経路が `:246` になっているが実際は `:276` | 対応。`:276` へ直し、`toStringDirectives` の呼び出しが `:214`・`:246`・`:276` の 3 箇所であること、`:246` が `readMessageBlock`（`DataType.MESSAGE`）の別経路であることを明記 | `grep -n 'toStringDirectives(' XlsFormatReader.java` → `214` `246` `276`（＋定義 `467`・`468`） |
| B-5 | COV-08 の引用が非逐語・節番号も誤り | 対応。「`inventory.md` **§3.3** が『軸要素の外に、開示すべき担保の穴が 1 つある。』として開示し、到達経路の全数と実測は §3.1-2 にある」へ直した | `inventory.md:2591` に当該文、直前の見出しは `:2545` `### 3.3 辺③ 未担保一覧`。`:2594` が §3.1-2 の該当項へリンク |
| B-6 | 「`##` 見出しのうち」がコマンドと合わない | 対応。「`##`〜`####` 見出しのうち」に直し、`§1-D`・`§1-E` が `####` であることを明記 | `grep -nE '^#{2,4} §[0-9]' issues.md` → `3093 ####`・`3188 ####`・`3371 ##`・`4054 ##` |
| B-7 ⚠ | COV-09 の引く「3 箇所」は**追補その 6**で 4 箇所に改められている | 対応。ただし**実物では追補その 5（2026-08-19 実測）で既に 4 箇所**である。注記は「追補その 5」と書いた | `inventory.md:677` の比較表見出しが `\| クラス \| 追補その 4 \| 追補その 5（現在） \|` で、その節（ブロック冒頭は `:573` `**追補（2026-08-19 実測。§1-B〜…の番人移設ぶん）**`）の辺④列挙（`:707-708`）が `#write`・`#emitBlock`・`#rawGroup`・`#sectionKey` の 4 件。`jacoco.xml` の `YamlFormatWriter.java` で `mb>0` の行は `84`・`139`・`484`・`503` |
| B-8 | 台帳の辺④列挙で `#sectionKey` の throw 行が落ちている | 対応。`inventory.md:708` と `:851`（同文）を「`#sectionKey`（default 分岐と throw 行）」へ直した | `YamlFormatWriter.java` の完全未到達行は `143` と `520` の 2 行。`:520` は `throw new IllegalArgumentException("unsupported DataType: " …)` |
| B-9 | 引用・行範囲の精度 3 件 | 対応。(1) §6-1 の XLS-09 引用に括弧句「（`XlsFormatReaderTest` の `FakeTestDataReader`）」を戻した。(2) #3 のコメント位置を `:231-232` にし `:230` が `if (message == null) {` であることを添えた。(3) #29 を「**同型の**コメント」にし、例示ファイル名の違い（`"foo.xlsx"` ／ `"foo.yaml"`）を明記 | `issues.md:615-616` と逐語照合。`XlsFormatReader.java:230-233` を `sed -n` で確認。`XlsFormatWriter.java:106` ／ `YamlFormatWriter.java:83` |
| B-10 | XLS-39 の `rawGroup` 出典が開けない | 対応。`YamlFormatWriter.java:479-488`（判定と `substring` は `:484-486`）へ直し、同ファイルが 524 行しかなく初版の `:526-535` は開けなかった旨の訂正注記を添えた | `wc -l YamlFormatWriter.java` → 524。`sed -n '479,488p'` が `rawGroup` の全体。逆写像 `YamlFormatReader.java:485-487` の `formatGroup` も確認 |
| C-1 | #5 の「存在しない」が過大 | 対応。「`HeaderCollector` が `DEFAULT` を落とすため、本経路では生じない」へ改め、§6-5 に (b) を新設して #5 を加えた（§6-5 の対象は 4 件 → **5 件**） | `TestCoreReaderAdapter.java:37` `public class`、`:238` `public List<BlockHeader> readHeaders(`、`:362-364` の `continue`、`BlockHeader.java:29-33` のパッケージプライベート ctor（検証なし）、`XlsFormatReader.java:89` の注入 ctor |
| C-2 | ラベル「実 `.xlsx` 経路では生じない」が同じ行の根拠に否定されている | 対応。ラベルを `テスト不要（実 .xlsx では生じない／Fake から到達可）` へ改め、凡例の意味欄も断定と例外を並べた。**正規表現も追随させ実行して 4 件で一致を確認** | `grep -cE '^\| [0-9]+ \|.*テスト不要（実 `\.xlsx` では生じない／Fake から到達可）'` → 4 |
| C-3 | 「テスト不要」が §3 の分岐と §4 の行の 2 母集団に使われている | 対応。§3.0 の断定に「**§3 の分岐 34 件については**」と範囲を切り、§4 の 8 行には「**テストで到達させられない**」という別の語を当てて「5 値の外である」と明記した | §4 の 8 行（`TestCoreReaderAdapter.java` `380`・`385`・`389`・`394`・`474`・`479`・`483`・`488`）を `jacoco.xml` で確認。ソースの `:375-376` ／ `:469-470` のコメントを逐語照合 |
| C-4 | 冒頭の「4 辺の担当クラス」が本書の区分定義と食い違う | 対応。冒頭を「**対象 6 区分（§1）の未到達分岐は 34 件**」へ直し、§3 に「（辺②は未到達分岐 0 件のため節を置かない。§1 の区分別合計）」を足した | §1 の区分別合計 `awk` の出力で ② が `missed line 0 branch 0` |
| D-1 | §6-5 の「他の 4 区分」が §1 の「対象 6 区分」と衝突 | 対応。「他の 4 つの理由」へ。`grep` で他に同種の用法が無いことを確認 | `grep -n '他の 4 区分\|他の 5 区分' coverage-report.md` → ヒット 0 |
| D-2 | 引用内の太字が引用者によるものだと断っていない | 対応。§3 冒頭の出典表記の約束に「**鉤括弧・`>` で囲んだ引用は逐語であり、引用中の太字は引用者による**（`notation` の原文にも `src/main` のコメントにも強調の書式は無い）」を足した | `notation:622`・`:883`・`:1325` を `30a8271` で開き `**` が無いことを確認 |
| D-3 | `§6-n` の宛先が本文に無い | 対応。§6 の 6 項すべての先頭に `**§6-1 …**`〜`**§6-6 …**` を置いた | `grep -c '\*\*§6-[1-6] ' coverage-report.md` → 6 |
| D-4 | §6 の見出しが §6-3 を覆っていない | 対応。「## 6. 開示：担保の穴・台帳の訂正・確かめきれなかったこと」へ | 同見出し行 |
| D-5 | #31・#34 の根拠欄が COV-13・COV-11 と重複 | 対応。2 件に限って根拠欄を「分岐が未到達である理由」に絞り、末尾を `→ issues.md COV-13` ／ `→ issues.md COV-11` の参照にした。表全体の再構成はしていない | `issues.md` の COV-11・COV-13 に論証と実装者向けの制約が残っていることを確認 |
| D-6 | `inventory.md:326` の再現性主張が現在は偽 | 対応。「**記録日（2026-08-14）時点の実測**であり、**現在の実行結果とは一致しない**」へ限定し、続く `@Ignore` 2 件の記述も過去形にして、**現在の 2 件は `coverage-report.md` §0 が正である**旨のポインタを張った | 現在の `@Ignore` は `YamlFormatReaderInvalidInputTest.java:740`（YML-14）と `:1280`（XLS-40）。`grep -l 'Skipped: [1-9]' target/surefire-reports/*.txt` は同クラス 1 件 |
| D-7 | 「以降の追補（その 4〜その 7）」が実物と違う | 対応。「行番号が見えるのは本節と追補その 5・その 6・その 7 の 4 か所」へ直した | 行番号を含む ② は `:340-341`・`:580-582`・`:802-804`・`:884-886` の 4 か所。追補その 2〜その 4 の ② は `:437`・`:477`・`:523` でいずれも「（ヒット 0 件）」 |
| D-8 | 「出力そのもの」が折り返し・省略を隠している | 対応。「`grep -rn` の出力である（**長い行は折り返し・省略してある**）」へ | 追補その 5〜その 7 の ② が 2 行の出力を 4 行に折り返し、`@Ignore` 文言を `…` で省略していることを確認 |
| D-9 | 凡例と §7 冒頭の三重複 | 対応。§7 冒頭を「**`COV-nn` の性格・欄・件数の扱いは、上部の凡例の `COV-nn` 行に定義がある。**」＋出典の 1 文へ縮め、裸の `§n` の注は残した | 凡例 `issues.md:31` に 3 点すべてが書かれていることを確認 |
| D-10 | 冒頭 2 か所の読みにくさ | 対応。(a)「未到達行は 16 件で、**うち 8 行は上の分岐の裏面である**（`throw` ／ `return` 行。§4）」。(b)「**14 件の課題**（`COV-01`〜`COV-14`）としてまとめた（1 課題が複数分岐にまたがる）」 | §4 の対応列で 8 行が §3 の分岐に紐づくことを確認。COV 項数 14 を実行して確認 |
| D-11 | `@Ignore` 表の他責先の呼び分け | 対応。YML-14 の他責先を「`nablarch-testing` の `DataFileFragment`（`addValue` が余りの値を捨てる）」に揃えた（§6-6 も同文に） | XLS-40 側が既に「`nablarch-testing` の `TableData`」 |
| D-12 | (a) §1 注記が出典より強い／(b)「型の全数分岐」の意味欄が循環 | 対応。(a)「原因は入力の作り方の偏りと**アサートの弱さ**で、どちらもカバレッジでは検出できない」。(b)「取りうる値を上ですべて分岐し尽くしているため、残る側へ入る値が型・列挙として存在しない（sealed 階層／`DataType`）」 | `inventory.md` の 2 巡目の原因記述（順序アサートのキー・種別名・送信系の入力）と閉じ方（テスト 1 件追加とアサート 2 か所の追加・入力の並べ替え）を確認 |

### Method を適用した記録

**書きながら出典に当たった。**確認したものは次のとおり（すべて自分で実行・自分で開いた）。

- **併記した導出コマンドは全件そのまま実行し、宣言値と一致することを確かめた**——`coverage-report.md` §0 の 3 本（`git diff` → 0 ／ `.gitignore` → `3:jacoco.exec` ／ `md5sum` → `d28e374e9027ade63d7919f7a7b5826e`）・`grep -l 'Skipped: [1-9]'` ／ `grep -rn '@Ignore'`、§1 の 3 本（クラス別 `awk` ／ 区分別合計 `awk` ／ 中間モデル 12 クラス）、§2 の Python、§3 の 2 本、§3.0 の 5 本、§6-2 の 2 本、§6-3 の 2 本、`issues.md` の 8 本（55／55／25／27／2／1／41／14）と §7 の 3 本（14／14／19）と `grep -nE '^#{2,4} §[0-9]'`
- **逐語引用を 1 文字ずつ照合した**——`notation:252`・`:254`・`:256`・`:266`・`:267`・`:306`・`:622`・`:883`・`:889`・`:1535`（すべて `git -C ../nablarch-document show 30a8271:…/testdata_notation.rst | sed -n 'Np'` でピン留め版から取得）、`issues.md:615-616` の XLS-09、`issues.md:646` の C-20、`XlsFormatReaderInvalidInputTest` の 2 つの Javadoc、`XlsFormatReader.java:231-232` ／ `:360` ／ `:445` ／ `TestCoreReaderAdapter.java:375-376` ／ `:469-470` のコメント
- **行番号は実際にその行を開いて確かめた**——`XlsFormatReader.java` `89`・`157`・`162`・`193`・`210`・`212`・`230-233`・`236`・`239-241`・`272-278`・`467-476`・`482`・`493-494`・`507`・`510-511`・`539-544`・`566-574`・`583-590`、`XlsFormatWriter.java:106`、`YamlFormatWriter.java` `83`・`479-488`、`YamlFormatReader.java:485-487`、`FieldDef.java:83-84`、`TestCoreReaderAdapter.java` `37`・`238`・`264`・`282-286`・`362-364`、`BlockHeader.java:29-33`、`XlsFormatReaderTest.java` `54`・`121-124`・`141`・`153`・`155`・`171`・`179`・`196`・`204`・`567`・`584`
- **JaCoCo の行単位の値は `jacoco.xml` を走査して確かめた**——`mb>0` の行は `XlsFormatReader.java` 21 行 ／ `XlsFormatWriter.java` 3 行（`107`・`202`・`557`）／ `YamlFormatWriter.java` 4 行（`84`・`139`・`484`・`503`）／ `YamlFormatReader.java` 0 行。`L157` は `mb=0 cb=2`、`L585` は `mb=1 cb=3`
- **確かめていないことは「未確認」と明示したまま**——(1) `-o` を外した場合の再現（§0 の注記・§6-4）、(2)「内部整合性ガード」に到達する canned 入力（器側と生行側を食い違わせるもの）を実際に組めるかどうか（§6-5(a)）

### 反映後に流し直した導出コマンドと出力（3 巡目・現在値）

```
$ F=.rn/ntf-test-data-converter/coverage/coverage-report.md
$ grep -cE '^\| [0-9]+ \|.*\*\*テストを足すべき\*\*' $F
19
$ grep -cE '^\| [0-9]+ \|.*テスト不要（' $F
15
$ grep -c 'テスト不要（' $F        # 素朴版（説明文とコマンド行自身がヒットする）
22
$ grep -cE '^\| [0-9]+ \|.*テスト不要（型の全数分岐）' $F
3
$ grep -cE '^\| [0-9]+ \|.*テスト不要（中間モデルの不変条件）' $F
2
$ grep -cE '^\| [0-9]+ \|.*テスト不要（内部整合性ガード）' $F
4
$ grep -cE '^\| [0-9]+ \|.*テスト不要（実 `\.xlsx` では生じない／Fake から到達可）' $F
4
$ grep -cE '^\| [0-9]+ \|.*テスト不要（本体パーサが先に弾く）' $F
2
$ I=.rn/ntf-test-data-converter/coverage/inventory.md
$ grep -cE '\.java:[0-9]+|\bL[0-9]{2,4}\b' $I
8
$ grep -E '\.java:[0-9]+|\bL[0-9]{2,4}\b' $I | grep -cE '^(② |   )src/test/java/'
8
$ J=.rn/ntf-test-data-converter/coverage/issues.md
$ grep -c '^### COV-' $J ; grep -c '^- 未到達箇所' $J ; awk -F'|' '/^\| COV-[0-9]+ \|/ { s += $4 } END { print s }' $J
14
14
19
$ grep -c '^### \(XLS\|YML\)-' $J ; grep -c '^### XLS-' $J ; grep -c '^### YML-' $J
55
41
14
$ git diff HEAD -- src/main src/test pom.xml | wc -l
0
$ md5sum target/site/jacoco/jacoco.csv
d28e374e9027ade63d7919f7a7b5826e  target/site/jacoco/jacoco.csv
```

§1 の区分別合計 `awk`・§2 の Python・§6-2 の 2 本の `awk` も流し直し、文書に貼ってある出力ブロックと
1 文字ずつ一致することを確かめた（区分別合計の末尾行は `合計 classes 22 line 943/959 branch 493/527
missed line 16 branch 34`、§6-2 の内訳は `計 14 クラス missed_line=63 missed_branch=16`）。

**本ファイルで触れたのは Completion Criteria 表の Self-check 欄・Evidence 欄、Overall Verdict の
`Self-check:` 行、冒頭の反映範囲の説明、そして本節の追記だけである。**
QA 欄・QA Expert Review 節・Expert Reviews 節・その他の Overall Verdict 行には触れていない。
**本ファイルはステージもコミットもしていない**（`git status --porcelain` に `??` として残る）。

### 適用漏れの補修（R1・R2）と機械的な洗い出し

コーディネータの独立レビューで、**既に採用した指摘の 2 つめの出現箇所**が 2 件見つかったため塞いだ。

| # | 指摘 | 対応 | 証拠 |
|---|---|---|---|
| R1 | B-10 の 2 つめ。`issues.md:3668` が `YamlFormatWriter.java:526-535` のまま | 対応。`:3624` と同じ `YamlFormatWriter.java:479-488` へ揃えた（訂正の断りは `:3624` にあるので二重には書かない） | `wc -l < YamlFormatWriter.java` → `524`。`sed -n '479p;484p;485p;488p'` が `private static String rawGroup(String groupId) {` ／ `if (groupId.charAt(0) == '[' && groupId.charAt(last) == ']') {` ／ `return groupId.substring(1, last);` ／ `}` |
| R2 | B-9(c) の 2 つめ。`issues.md:4210` が「同じコメントがある」のまま | 対応。`coverage-report.md` §3.3 #29 と同じ「**同型の**」へ揃え、例示ファイル名の違い（`"foo.xlsx"` ／ `"foo.yaml"`）も併記した | `sed -n '106p' XlsFormatWriter.java` と `sed -n '83p' YamlFormatWriter.java` が `例: "foo.xlsx"` ／ `例: "foo.yaml"` の違いだけで一致 |

**同種の取りこぼしを機械的に洗った。残りは無い。**

```
$ grep -rn 'YamlFormatWriter\.java:[0-9]\|XlsFormatWriter\.java:[0-9]\|YamlFormatReader\.java:[0-9]\|XlsFormatReader\.java:[0-9]' .rn/ntf-test-data-converter/coverage/
（39 行ヒット。全件の行番号を実物で開いて確認した。結果は下記）
$ grep -rn '同じコメント' .rn/ntf-test-data-converter/coverage/
（ヒット 0 件）
```

1 本目に加えて、**`coverage/` 全文の `<Name>.java:<行>` 形式を機械的に走査**し、リポジトリ内のファイルは
実ファイルの行数と突き合わせた（同名ファイルの重複は 0 件）。

- **範囲外は 1 件だけ**。`issues.md:3627`（`YamlFormatWriter.java:526-535`）——これは**訂正の断りの中で
  「初版はこう書いていた」と引いている誤りの値そのもの**で、同じ行が続けて「同ファイルは 524 行しかなく
  出典が開けなかった」と述べている。直さない。
- `issues.md:1267` の `XlsFormatWriter.java:380-381` ／ `YamlFormatWriter.java:354-358` は
  「**移設前**の未到達は…であった」と過去時点を明示しているため対象外。直さない。
- `inventory.md` の `…java:638` ／ `:1001` ／ `:740` ／ `:1277` ／ `:1280` は**凍結した `grep -rn` 出力ブロックの中**（§0.1-2 と追補その 5〜その 7）であり対象外。現在の `@Ignore` 実測は `:740`・`:1280` の 2 件で一致する。
- 本リポジトリに無い 4 ファイルは**ピン留めした版で照合した**——`PoiXlsReader.java:123`（`nablarch-testing` `c5f3340`。`String cellValue = cell == null ? "" : cell.toString();`）／`NablarchTestUtils.java:251-263`（同。`trimTail` の宣言〜閉じ括弧）／`HeaderLine.java:59-67`（同。`getMapExcludingMarkerColumns` の宣言〜閉じ括弧）／`YamlSection.java:112-114`（`nablarch-testing-yaml` `0197071`。`toStr` の `return value != null ? value.toString() : null;`）。

追加で流した形も 0 件または妥当だった——`同じ文言` は `coverage-report.md:306` の 1 件のみで、
`XlsFormatReader.java:360` と `:445` が**バイト一致のコメント**であることを実物で確認済み。
`526-535` の残りは上記 `issues.md:3627` のみ。
