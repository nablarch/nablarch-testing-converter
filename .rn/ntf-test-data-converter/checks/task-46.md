# #46 Completion Check

指示書 第2回「4. 完了条件」の残りと「6. 報告」（`nablarch-document@a12fb67`）。

**レビュア subagent は回していない**（Rules「#30 以降、レビュア subagent は回さない」）。

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| `mvn -o clean test` が緑・`@Ignore` 0 件 | OK | `Tests run: 678, Failures: 0, Errors: 0, Skipped: 0` ／ `BUILD SUCCESS`。`grep -rn '^    @Ignore' src/test --include=*.java` → 0 件 |
| カバレッジ C0/C1 の計測結果が報告にある | OK | `checks/step4-2-report.md` §6（着手前と完了時の 2 回を同じ手順で計測して突き合わせた） |
| 報告の 7 項がすべて埋まっている | OK | 同 §1〜§7 |
| `git status --short` が空 | OK | 本コミット後に無出力 |
| push 済み | OK | 本コミットを `origin/ntf-test-data-converter` へ push |

## ゲート（Rules「#30 以降」）

- `git diff --stat 26701b7..HEAD -- src/`: **`src/` の追跡ファイルは無変更。**
  追跡していなかった測定用の一時テスト 2 ファイル（`core/reader/ZzOracleProbeTest` ／
  `xls/ZzProbeTest`）を削除した（`git status` から消えるだけで、差分には現れない）。

- `mvn -o clean test` の最終行:

  ```
  Tests run: 678, Failures: 0, Errors: 0, Skipped: 0
  BUILD SUCCESS
  ```

  着手時点の 656 件・赤 4 件から、**＋22 件・赤 0 件**になった。

## 完了条件の突き合わせ（指示書「4. 完了条件」11 項）

| # | 完了条件 | 判定 | 置き場所 |
|---|---|:-:|---|
| 1 | 第2節の 6 件がすべて是正され、是正ごとに直す前は落ちて直したあとは通るテストがある | ✅ | 報告 §2 の表（「直す前に落ちたテスト」列）。`d611bec` を worktree に出しての実測は `checks/task-41.md` ／ `checks/task-44.md` |
| 2 | 2-1・2-3 の「着手前に特定すること」の結果が実装前に報告されている | ✅ | 報告 §1（`c10de5e`。承認は指示書 §8） |
| 3 | 2-5 の 4 経路テストが本体を正解にしており、母集合に 4 種が入っている | ✅ | 報告 §3 |
| 4 | 足したテスト・直したテストそれぞれについて期待値を崩すと落ちることを確認している | ✅ | 報告 §4（変異 34 件）。全件表は各 `checks/task-4N.md` |
| 5 | 既存テストの期待値を変えた箇所が全件挙がっている | ✅ | 報告 §5（変えた 7 件／変えなかったが緑へ変わった 3 件／変えなかったもの） |
| 6 | `@Ignore` が 0 件 | ✅ | 上表 |
| 7 | カバレッジ C0/C1 を計測し、下がった箇所があれば挙げる | ✅ | 報告 §6。**下がった箇所は無い**（全 53 クラスで未到達の行・分岐がいずれも増えていない） |
| 8 | `mvn -o clean test` が緑 | ✅ | 上表 |
| 9 | `git status --short` が空 | ✅ | 一時ファイル 2 件を削除。`jacoco.exec` も削除した |
| 10 | 変更を push する | ✅ | 本コミット |
| 11 | 2-6 の 2 つの grep が 0 件で、2-6 が単独のコミットになっている | ✅ | 報告 §7。コミットは `26701b7` |

## カバレッジの計測について

**着手前（`d611bec`）と完了時（`26701b7`）の 2 回を同じ手順で計測した。**
着手前の値は worktree（`git worktree add --detach <dir> d611bec`）で測っており、
第1回の報告 §6-3 が記録した値（行 1632/1706 ＝ 95.66%／分岐 761/818 ＝ 93.03%）と一致した。
同じ手順・同じ対象であることの確認になっている。

完了時は 行 **1632/1704 ＝ 95.77%**／分岐 **763/810 ＝ 94.20%**（`jacoco.csv` md5
`0ea76427cb5afd322a2c3804ea30bf13`）。**下がったクラスは 1 つも無く**、`XlsFormatReader` は
未到達分岐が 25 → 15 へ、未到達行が 5 → 3 へ減った。

**`coverage/coverage-report.md` は書き換えていない。** 同書の全数値は `da66425` の 1 回の実行に
固定したままである（Rules「JaCoCo の再計測はしない」の趣旨は同書が引く行番号を自己無効化させないこと。
本節の数値は別の実行であり、時点の証拠記録である `checks/` に置いた）。

## 台帳の更新

- `coverage/inventory.md` §0.1-2 に**追補その 18**（一時テスト削除により ① と追跡対象が一致。678 件）

## 調整側へ開示するもの（マージ可否の判断は出さない）

Rules「マージ可否の判断は出さない。完了報告に『要対応 0 件』と書かない」に従う。開示は次の 4 件。

| # | 内容 | 置き場所 |
|---|---|---|
| 1 | 指示書 §8-5 の「死んだコード」のうち `tail` は死んでいなかった（4 か所から呼ばれている）。残した | 報告 §2「指示書との食い違い」 |
| 2 | `b7d2320` の時点で送信同期系だけインタープリタ列が渡っていなかった。#40 で直した | 同上 |
| 3 | 送信同期の末尾 `null`（2-1 実測表の S2）は 4 経路の母集合ではなく直接の突き合わせで押さえた | 報告 §3 |
| 4 | 第1回から持ち越している未到達分岐 2 件（`XlsFormatWriter#isQuotationWrapped` の全角クォート側 3 分岐／`TestCoreReaderAdapter#markerGroupId` の角括弧が閉じていない側 2 分岐）は今回も未到達。第2回の完了条件外 | 報告 §6 |
